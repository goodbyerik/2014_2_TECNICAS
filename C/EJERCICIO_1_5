{
	int i;
	for (i=0;i<5;i++){ 
		//por cada for, una llave
		printf("%d\n",i);
		printf("%d\n",v[i]);
		}
}

int main(int argc, char *argv[])
{
	int a[]={1,2,3,4,5};
	int *p;
	p=&a[0];
	imprimir(p);
	return 0;
}
